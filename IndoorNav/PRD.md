[← README](../README.md)

# PRD — IndoorNav

**Версия:** 2.1  
**Дата:** 07.03.2026  
**Статус:** Актуально

> **Изменения v2.1 относительно v2.0:**  
> Серверные ЧС-уведомления (ASP.NET Core Minimal API + polling) с полноэкранным оверлеем-спамом;  
> полный оборот карты на 360° (было ±25°); контр-ротация подписей и иконок узлов при повороте;  
> ограничение зума по зданиям (Верхний корпус 0.7×, Нижний корпус 0.88×);  
> ограничение панирования — горизонтально 20% ширины экрана, вертикально 60% карты не скрывается.

---

## 1. Обзор продукта

**IndoorNav** — мобильное/десктопное приложение для навигации внутри здания (университет). Строится на базе **.NET 9 MAUI** с SkiaSharp-рендерингом SVG-планов этажей, алгоритмом Дейкстры для прокладки маршрутов и встроенным редактором карты.

### Целевые платформы

| Платформа | Статус |
|-----------|--------|
| Windows 10/11 (unpackaged) | ✅ Основная |
| Android | ✅ Поддерживается |
| iOS / macOS Catalyst | ✅ Требует Mac + Xcode |

---

## 2. Цели и задачи

| Цель | Метрика успеха |
|------|----------------|
| Пользователь находит маршрут до нужной аудитории | ≤ 3 тапа от запуска |
| Студент видит, где проходит его текущая пара | Расписание подтягивается автоматически по группе |
| Администратор редактирует карту без перекомпиляции | Сохранение за ≤ 5 секунд через UI |
| ЧС-оповещение доходит до всех пользователей | Мгновенно после активации администратором |

---

## 3. Пользователи

### 3.1 Гость (Guest)
- Анонимный вход без пароля
- Полный доступ к навигации и QR-сканеру
- Нет доступа к расписанию групп и административным функциям

### 3.2 Студент (Student)
- Вход по логину/паролю
- Привязан к учебной группе и кафедре
- Видит текущую и следующую пару для своей группы
- Может строить маршруты до аудитории из расписания

### 3.3 Администратор (Admin)
- Полный доступ: редактор карты, управление пользователями, отделами, расписанием
- Активирует/деактивирует режим ЧС по зданиям
- Публикует обновления карты через GitHub Gist (опционально)

---

## 4. Функциональные требования

### 4.1 Авторизация

| ID | Требование |
|----|-----------|
| AUTH-1 | Вход по логину + паролю (SHA-256 хэш, хранение в `users.json`) |
| AUTH-2 | Вход без аккаунта (гость) одним кликом |
| AUTH-3 | По умолчанию существует учётная запись администратора `0` / `0` |
| AUTH-4 | Администратор меняет пароль любому студенту; студент меняет только свой |
| AUTH-5 | Сессия восстанавливается после перезапуска через `Preferences` |
| AUTH-6 | Разделение ролей: `Admin`, `Student`, `Guest` |

---

### 4.2 Навигация

| ID | Требование |
|----|-----------|
| NAV-1 | Выбор начальной и конечной точки через поиск по списку узлов |
| NAV-2 | Автодополнение поиска по имени, тегам, категории узла |
| NAV-3 | Построение маршрута алгоритмом Дейкстры через граф (`NavGraph`) |
| NAV-4 | Поддержка многоэтажных маршрутов через лестницы/лифты (`IsTransition`) |
| NAV-5 | Переходные узлы группируются по `TransitionGroupId`; штраф за смену этажа — 150 у.е. |
| NAV-6 | Пошаговые инструкции: текстовые подсказки + иконки; анимированный фокус на текущем шаге |
| NAV-7 | Счётчик шагов «N / M»; кнопки «Далее» / «Назад» |
| NAV-8 | Анимированный маршрут на карте (пунктир с бегущей точкой) |
| NAV-9 | Тап по узлу на карте открывает popup с именем, типом и кнопками «Отсюда» / «Сюда» |
| NAV-10 | Запасные выходы (`IsEvacuationExit`) исключаются из обычной маршрутизации — доступны только в режиме ЧС |
| NAV-11 | Обычные выходы (`IsExit`) также исключены из транзитных узлов маршрута |
| NAV-12 | Режим блокировки: пользователь указывает непроходимый узел → маршрут перестраивается в обход (`_blockedNodeIds`) |
| NAV-13 | Узлы-waypoints маршрута отображаются в режиме блокировки для выбора заблокированной точки |
| NAV-14 | Боковая панель (sidebar) на десктопе сворачивается/разворачивается кнопкой `☰` / `‹` |

---

### 4.3 QR-навигация

| ID | Требование |
|----|-----------|
| QR-1 | Каждый `IsQrAnchor`-узел имеет QR-код формата `indoornav://node/{guid}` |
| QR-2 | Сканирование QR показывает «Вы тут» баннер — пользователь подтверждает точку как начало маршрута |
| QR-3 | Поддержка legacy URL: `indoornav-node:{guid}` и bare GUID |
| QR-4 | На Android/iOS — `QrScanPage` с двумя кнопками: «Сфотографировать QR» (камера) и «Выбрать из галереи» (файл) |
| QR-5 | На Windows — `FilePicker` для выбора изображения с QR; декодирование через ZXing `RGBLuminanceSource` + `MultiFormatReader` (с `TRY_HARDER`) |
| QR-6 | После сканирования карта переключает здание/этаж и зумирует к узлу (масштаб 3.0×) |
| QR-7 | Баннер «Вы здесь» в нижней части экрана: имя узла + кнопки «Начать отсюда» и «✕» |
| QR-8 | На карте над QR-узлом (пока он `PendingQrNode`) рисуется бирюзовый треугольник + таблетка «Вы тут» |
| QR-9 | После подтверждения (`ConfirmQrStartCommand`) узел становится `StartNode`; он остаётся видимым на карте независимо от флага `QrAnchorNodeIds` |
| QR-10 | Deep link обрабатывается через `DeepLinkService.RequestNode` (событие, подписка в VM) |
| QR-11 | Администратор размещает QR-якорь: если узел попадает вблизи (≤ 20 SVG-единиц) существующего ребра, ребро автоматически разрезается через новый узел (`TrySnapQrNodeToEdge`) |

---

### 4.4 Карта

| ID | Требование |
|----|-----------|
| MAP-1 | Рендер SVG плана этажа через SkiaSharp + Svg.Skia |
| MAP-2 | Наложение узлов, рёбер, границ комнат поверх SVG |
| MAP-3 | Пан (перетаскивание) + зум (mouse wheel / pinch); VSync-синхронизированный рендер |
| MAP-4 | Выбор здания через всплывающий picker прямо на карте (`BuildingPickerItemVm`); выбор этажа через оверлей-селектор (`FloorSelectorVm`) |
| MAP-5 | Разные визуальные стили для типов узлов: комната, переход, выход, огнетушитель, эвакуационный выход, QR-якорь |
| MAP-6 | Цвет узла настраивается (`NodeColorHex`); масштаб и метка — отдельно; внутренняя надпись/эмодзи (`InnerLabel`) |
| MAP-7 | Огнетушители и запасные выходы скрыты в обычном режиме; видны при `ShowFireExtinguishers` (ЧС) |
| MAP-8 | Полигональные границы комнат (`Boundaries`) отображаются и редактируются |
| MAP-9 | Узлы с `IconPath` отображаются как PNG-иконки вместо кружков; иконки загружаются асинхронно с кешированием (`ConcurrentDictionary`) и фолбэком к бандл-пути `Icons/filename` |
| MAP-10 | Алгоритм антиперекрытия меток: подписи, пересекающие уже нарисованные, пропускаются (кроме приоритетных узлов) |
| MAP-11 | Индикатор «Вы тут» над `PendingQrNode`: бирюзовый треугольник-стрелка + таблетка с текстом |
| MAP-12 | Поворот карты на произвольный угол (зажаты ЛКМ + ПКМ): полные 360° без ограничения |
| MAP-13 | Подписи и иконки узлов контр-ротируют вместе с картой — всегда остаются горизонтальными для читаемости |
| MAP-14 | Максимальный зум ограничен по зданиям: Верхний корпус (`BuildingA`) — 0.7×, Нижний корпус (`BuildingB`) — 0.88×, остальные — 1.0× |
| MAP-15 | Ограничение панирования: горизонтально — карта не может уйти за пределы так, чтобы менее 20% ширины экрана оставалось под картой с каждой стороны; вертикально — не менее 40% высоты карты должно оставаться видимым |

---

### 4.5 Расписание

| ID | Требование |
|----|-----------|
| SCH-1 | Хранение расписания в `schedule.json` (день недели + временные слоты) |
| SCH-2 | Автоопределение текущей пары для комнаты или группы |
| SCH-3 | Студент видит свою текущую аудиторию и может проложить маршрут одним нажатием |
| SCH-4 | Администратор управляет расписанием: добавление/удаление/редактирование записей |
| SCH-5 | Просмотр расписания по комнатам или по группам |
| SCH-6 | Фильтрация расписания по группе |

---

### 4.6 Режим ЧС

| ID | Требование |
|----|-----------|
| EMRG-1 | Администратор активирует/деактивирует ЧС отдельно по каждому зданию |
| EMRG-2 | При активации все пользователи немедленно видят баннер с сообщением |
| EMRG-3 | В режиме ЧС строится маршрут до ближайшего выхода (`IsExit` или `IsEvacuationExit`) |
| EMRG-4 | Система запоминает, какие узлы заблокированы (`_blockedNodeIds`) — исключает их из роутинга |
| EMRG-5 | Пользователь может указать своё местоположение тапом при активации ЧС |
| EMRG-6 | Состояние ЧС сохраняется в `emergency_state.json` и восстанавливается после перезапуска |
| EMRG-7 | В ЧС-режиме на карте появляются огнетушители и запасные выходы |
| EMRG-8 | Состояние ЧС публикуется на локальный ASP.NET Core сервер (`http://localhost:5180`); клиент опрашивает сервер каждые 30 с. через `PeriodicTimer`; при расхождении состояний — автоматическая синхронизация |
| EMRG-9 | При получении ЧС-сигнала приложение запускает полноэкранный оверлей (ZIndex=200, мигает каждые 4 с.) до подтверждения пользователем; после подтверждения — переход к выбору местоположения для эвакуационного маршрута |

---

### 4.7 Администраторский редактор карты

| ID | Требование |
|----|-----------|
| ADMIN-MAP-1 | Добавление узлов разных типов: комната, переход, лифт, лестница, выход, запасной выход, огнетушитель, QR-якорь |
| ADMIN-MAP-2 | Соединение/разъединение узлов рёбрами (режим Connect / Disconnect) |
| ADMIN-MAP-3 | Рисование коридора — последовательность рёбер за одну операцию |
| ADMIN-MAP-4 | Перемещение узлов drag-and-drop |
| ADMIN-MAP-5 | Рисование полигональных границ комнат |
| ADMIN-MAP-6 | Редактирование свойств узла: имя, внутренний ярлык, теги, категория, цвет, масштаб, флаги |
| ADMIN-MAP-7 | Генерация QR-кода для узла; копирование URL; скачивание PNG |
| ADMIN-MAP-8 | Удаление узла вместе с его рёбрами |
| ADMIN-MAP-9 | Сохранение в `navgraph.json`; опционально — публикация в GitHub Gist |
| ADMIN-MAP-10 | Множественный выбор узлов (Ctrl+клик, rubber-band прямоугольником); групповые операции: удаление, назначение иконки, сброс стиля |
| ADMIN-MAP-11 | Назначение PNG-иконки узлу через FilePicker (абсолютный путь нормализуется к `Icons/filename` для бандла) |
| ADMIN-MAP-12 | При размещении QR-якоря — автоматическое разрезание ближайшего ребра графа, если расстояние от нового узла до ребра ≤ 20 SVG-единиц (`TrySnapQrNodeToEdge`) |

---

### 4.8 Управление пользователями

| ID | Требование |
|----|-----------|
| USR-1 | Администратор создаёт студентов с привязкой к учебной группе |
| USR-2 | Удаление и редактирование студентов |
| USR-3 | Смена пароля через диалог |
| USR-4 | Просмотр списка студентов с фильтрацией по кафедре |

---

### 4.9 Управление кафедрами и группами

| ID | Требование |
|----|-----------|
| DEPT-1 | Иерархия: Кафедра → Учебные группы |
| DEPT-2 | CRUD операции для кафедр и групп через AdminPage |
| DEPT-3 | Данные хранятся в `departments.json` |

---

## 5. Нефункциональные требования

| Категория | Требование |
|-----------|-----------|
| **Производительность** | Рендер карты синхронизирован с VSync (частота экрана); одного мастер-таймера достаточно для анимации маршрута + пана |
| **Надёжность** | Все данные хранятся локально; потеря сети не прерывает навигацию |
| **Безопасность** | Пароли хранятся как SHA-256 хэш; токены GitHub не публикуются в коде (поле пустое) |
| **Offline** | Полная работостособность без интернета; Gist-синхронизация — опциональная функция |
| **Расширяемость** | Новые здания добавляются через JSON + SVG без перекомпиляции |
| **UX** | Время от запуска до построения маршрута — не более 3 кликов |
| **Размер** | Приложение работает без MSIX-упаковки (`WindowsPackageType=None`) |

---

## 6. Архитектура

### 6.1 Паттерн
MVVM (Model-View-ViewModel) с `INotifyPropertyChanged` и `Command`-паттерном.

### 6.2 Слои

```
┌─────────────────────────────────────────┐
│               UI Layer                  │
│  MainPage · AdminPage · LoginPage ·     │
│  QrScanPage · SvgView (SkiaSharp)       │
├─────────────────────────────────────────┤
│           ViewModel Layer               │
│  MainViewModel · AdminViewModel ·       │
│  LoginViewModel                         │
├─────────────────────────────────────────┤
│            Service Layer                │
│  NavGraphService · AuthService ·        │
│  EmergencyService · ScheduleService ·   │
│  DepartmentService · QrService ·        │
│  DeepLinkService                        │
├─────────────────────────────────────────┤
│             Model Layer                 │
│  NavGraph · NavNode · NavEdge ·         │
│  Building · Floor · RouteStep ·         │
│  AuthUser · ScheduleEntry · Department  │
└─────────────────────────────────────────┘
```

### 6.3 Хранилище данных

| Файл | Содержимое | Расположение |
|------|-----------|-------------|
| `navgraph.json` | Граф навигации (узлы + рёбра) | `IndoorNav/Resources/Raw/` |
| `users.json` | Пользователи с хэшами паролей | `IndoorNav/Resources/Raw/` |
| `departments.json` | Кафедры и учебные группы | `IndoorNav/Resources/Raw/` |
| `schedule.json` | Расписание занятий | `IndoorNav/Resources/Raw/` |
| `emergency_state.json` | Состояние ЧС по зданиям | `IndoorNav/Resources/Raw/` |
| `Preferences` | Текущая сессия (userId, role) | MAUI Preferences |

---

## 7. Модели данных

### NavNode (ключевые поля)

| Поле | Тип | Описание |
|------|-----|---------|
| `Id` | string (GUID) | Уникальный идентификатор |
| `Name` | string | Отображаемое имя |
| `BuildingId` | string | Здание |
| `FloorNumber` | int | Этаж |
| `X`, `Y` | float | Координаты в SVG |
| `IsTransition` | bool | Переходный узел (лестница/лифт) |
| `IsElevator` | bool | Лифт |
| `TransitionGroupId` | string | Группа физического перехода |
| `IsQrAnchor` | bool | Точка входа через QR |
| `IsExit` | bool | Обычный выход |
| `IsEvacuationExit` | bool | Запасной эвакуационный выход |
| `IsFireExtinguisher` | bool | Огнетушитель |
| `IsRoom` | bool | Аудитория/помещение |
| `IsWaypoint` | bool | Путевая точка (без имени) |
| `IsHidden` | bool | Скрыт на карте |
| `IsLabelHidden` | bool | Скрыта подпись узла |
| `Category` | string | Категория (отображается в popup) |
| `IsCategoryHidden` | bool | Скрыть категорию в popup |
| `SearchTags` | string | Дополнительные теги для поиска |
| `NodeColorHex` | string | Цвет кружка `RRGGBB` без `#` |
| `NodeRadiusScale` | float | Множитель радиуса (по умолчанию 1.0) |
| `LabelScale` | float | Множитель размера подписи |
| `InnerLabel` | string | Текст/эмодзи внутри кружка |
| `IconPath` | string | Путь к PNG-иконке (`Icons/filename.png` или абсолютный) |
| `Boundaries` | List\<List\<float[]\>\> | Полигональные границы комнаты |

### NavEdge

| Поле | Тип | Описание |
|------|-----|---------|
| `FromId` / `ToId` | string | Идентификаторы узлов |
| `Weight` | float | Длина (авто или ручная) |
| `IsCrossFloor` | bool | Межэтажное ребро |

---

## 8. Пользовательские сценарии (User Flows)

### 8.1 Гость строит маршрут

```
Запуск → LoginPage → «Гость» → MainPage
  → Тап «Откуда» → Ввод поиска → Выбор узла
  → Тап «Куда» → Ввод поиска → Выбор узла
  → «Построить маршрут» → Анимированный маршрут на карте
  → Листание шагов инструкции
```

### 8.2 Студент находит свою аудиторию

```
LoginPage → «Пользователь» → логин/пароль → MainPage
  → Баннер «Сейчас: [аудитория]» → Тап → Маршрут проложен
```

### 8.3 QR-навигация

```
MainPage → кнопка 📷 → 
  Android/iOS: QrScanPage → «Сфотографировать QR» / «Выбрать из галереи»
  Windows:     FilePicker → выбор PNG/JPG с QR
  → QrService.DecodeFromBytes → DeepLinkService.RequestNode
  → HandleDeepLinkNode → переключить здание/этаж при необходимости
  → PendingQrNode установлен → карта зумирует к узлу (3.0×)
  → Баннер «Вы здесь: [имя узла]» + кнопка «Начать отсюда»
  → ConfirmQrStartCommand → StartNode = PendingQrNode
```

### 8.4 Режим ЧС

```
AdminPage → Управление ЧС → «Активировать» → все пользователи
  видят баннер → тап «Построить маршрут» → маршрут до
  ближайшего IsExit | IsEvacuationExit (заблокированные исключаются)
```

### 8.5 Администратор добавляет QR-якорь

```
AdminPage → вкладка «Карта» → «📱 QR-якорь»
  → тап на плане → NavNode{IsQrAnchor=true, InnerLabel="📱"} создан
  → если тап близко к существующему ребру (≤ 20 SVG-ед.)
     → TrySnapQrNodeToEdge: ребро A→B заменяется на A→QR + QR→B
  → «📱 Показать QR-код» → генерация PNG → копировать/скачать
```

### 8.6 Блокировка узла на маршруте

```
Маршрут построен → кнопка «Заблокировать» → IsBlockingMode=true
  → тап на непроходимый узел маршрута → PendingBlockNode
  → маршрут пересчитывается в обход
  → повторный тап другого узла — меняет заблокированный
```



## 9. Технический стек

| Технология | Версия | Назначение |
|-----------|--------|-----------|
| .NET MAUI | 9.0 | Кроссплатформенный UI фреймворк |
| SkiaSharp | 3.119.2 | 2D-рендеринг карты |
| Svg.Skia | 3.4.1 | Парсинг и рендер SVG планов |
| QRCoder | 1.4.3 | Генерация QR-кодов (PNG без GDI+) |
| ZXing.Net.Maui | 0.4.0 | QR-сканер (Android/iOS) |
| ASP.NET Core (Minimal API) | 9.0 | Локальный сервер ЧС-оповещений (`Tools/EmergencyServer`, порт 5180) |
| System.Text.Json | встроен | Сериализация/десериализация данных |

---

## 10. Ограничения и допущения

- Планы этажей хранятся как SVG-файлы в `Resources/Raw/SvgFloors/`; замена SVG требует пересборки
- GitHub Gist синхронизация (`AppConfig.cs`) отключена по умолчанию — поля пустые
- ZXing.Net.Maui подключается только на Android/iOS; на Windows QR сканируется вводом текста
- Координаты узлов привязаны к SVG-пространству 0–800 × 0–600; масштаб при пане/зуме — афинное преобразование

---

## 11. Известные ограничения текущей версии

| # | Описание |
|---|---------|
| 1 | `AppConfig.GistRawUrl` / `GistId` / `GithubToken` — пустые; синхронизация карты через облако отключена |
| 2 | QR-сканер через камеру доступен только на Android/iOS; на Windows — выбор изображения через FilePicker с ZXing-декодированием |
| 3 | ЧС-сервер (`Tools/EmergencyServer`) запускается отдельно на той же машине; клиент опрашивает его каждые 30 с. (нет background push) |
| 4 | Планы этажей хранятся как SVG-файлы в `Resources/Raw/SvgFloors/`; замена SVG требует пересборки |
| 5 | Координаты узлов привязаны к SVG-пространству; размеры SVG-карт не унифицированы между зданиями |

---

## 12. Будущие улучшения (Backlog)

- [ ] Облачная синхронизация пользователей и расписания (Firebase / REST API)
- [ ] Push-уведомления о ЧС через платформенные notification API
- [ ] Импорт/экспорт SVG с автоматической разметкой узлов
- [ ] Голосовые подсказки при навигации (TTS)
- [ ] История маршрутов
- [ ] Accessibility: поддержка TalkBack / VoiceOver
- [ ] Тёмная тема
- [ ] Bluetooth/UWB позиционирование для автоматического определения этажа
- [ ] Верификация пересечений рёбер при редактировании карты
